/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
  reactStrictMode: true,
  // Cloudflare Pages serves static files; we generate them into ./out
};

module.exports = nextConfig;
